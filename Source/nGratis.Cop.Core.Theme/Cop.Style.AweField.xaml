<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:core="http://schemas.ngratis.com/2014/core"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d">
  <ResourceDictionary.MergedDictionaries>
    <core:SharedResourceDictionary Source="/nGratis.Cop.Core.Theme;component/Cop.Style.AweFileDialogButton.xaml" />
    <core:SharedResourceDictionary Source="/nGratis.Cop.Core.Theme;component/Cop.Style.TextBlock.xaml" />
    <core:SharedResourceDictionary Source="/nGratis.Cop.Core.Theme;component/Cop.Style.TextBox.xaml" />
  </ResourceDictionary.MergedDictionaries>

  <core:FieldTemplateSelector x:Key="Cop.AweField.TemplateSelector" />

  <DataTemplate x:Key="Cop.AweField.Default" DataType="core:AweField">
    <TextBlock
      FontStyle="Italic"
      Style="{StaticResource Cop.TextBlock.Body}"
      Text="___UNSUPPORTED___" />
  </DataTemplate>

  <DataTemplate x:Key="Cop.AweField.Input.Text" DataType="core:AweField">
    <TextBox Style="{StaticResource Cop.TextBox.Normal}" Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
  </DataTemplate>

  <DataTemplate x:Key="Cop.AweField.Input.File" DataType="core:AweField">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBox
        x:Name="PART_Value"
        Grid.Column="0"
        Margin="0,0,5,0"
        Style="{StaticResource Cop.TextBox.Normal}"
        Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
      <core:AweFileDialogButton
        Grid.Column="1"
        Content="Browse"
        SelectedFilePath="{Binding Value, Mode=TwoWay}"
        Style="{StaticResource Cop.AweFileDialogButton.Normal}" />
    </Grid>
  </DataTemplate>

  <DataTemplate x:Key="Cop.AweField.Input.Range" DataType="core:AweField">
    <Grid DataContext="{Binding Value, Mode=OneWay}">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="25" />
      </Grid.ColumnDefinitions>
      <core:AweSlider
        x:Name="Slider"
        Grid.Column="0"
        IsSnapToTickEnabled="True"
        Maximum="{Binding MaximumValue, Mode=OneWay}"
        Minimum="{Binding MinimumValue, Mode=OneWay}"
        StableValue="{Binding CurrentValue, Mode=TwoWay}"
        TickFrequency="{Binding Interval, Mode=OneWay}"
        d:DataContext="{d:DesignInstance core:Range}" />
      <TextBlock
        Grid.Column="1"
        Margin="0,0,0,0"
        HorizontalAlignment="Right"
        Style="{StaticResource Cop.TextBlock.Body}"
        Text="{Binding Value, ElementName=Slider, Mode=OneWay}"
        d:DataContext="{d:DesignInstance core:Range}" />
    </Grid>
  </DataTemplate>

  <DataTemplate x:Key="Cop.AweField.Output.ReadOnlyText" DataType="core:AweField">
    <TextBlock Style="{StaticResource Cop.TextBlock.Body}" Text="{Binding Value, Mode=OneWay}" />
  </DataTemplate>

  <DataTemplate x:Key="Cop.AweField.Output.Image" DataType="core:AweField">
    <Image Source="{Binding Value, Mode=OneWay}" Stretch="UniformToFill" />
  </DataTemplate>

  <Style x:Key="{x:Type core:AweField}" TargetType="core:AweField">
    <Setter Property="Margin" Value="0,0,0,10" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="core:AweField">
          <StackPanel Orientation="Vertical">
            <TextBlock
              x:Name="PART_Label"
              Margin="0,0,0,5"
              Style="{StaticResource Cop.TextBlock.SmallSubheader}"
              Text="{Binding Label, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}" />
            <ContentControl Content="{Binding}" ContentTemplateSelector="{StaticResource Cop.AweField.TemplateSelector}" />
          </StackPanel>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>